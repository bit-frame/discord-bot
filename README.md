# Installation
This installation assumes that you already have a bot to use and required dependencies.

The scripts used in this installation can be located in the files section of this repo.

```
mkdir discord-bot
cd discord-bot
npm init -y
npm install discord.js dotenv
echo "TOKEN=your-bot-token-here" > .env
touch bot.js
node bot.js
```
